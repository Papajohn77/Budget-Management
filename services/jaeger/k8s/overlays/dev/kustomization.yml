apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
patches:
  - target:
      kind: Service
      name: jaeger-query
    patch: |-
      - op: replace
        path: /spec/type
        value: NodePort
