apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: piggybank-data
  labels:
    app: piggybank-service
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: piggybank-service
  labels:
    app: piggybank-service
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: piggybank-service
  template:
    metadata:
      labels:
        app: piggybank-service
    spec:
      containers:
        - name: piggybank-service
          image: papajohn77/piggybank-service
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          volumeMounts:
            - name: piggybank-data
              mountPath: /data
      volumes:
        - name: piggybank-data
          persistentVolumeClaim:
            claimName: piggybank-data
---
apiVersion: v1
kind: Service
metadata:
  name: piggybank-service
  labels:
    app: piggybank-service
spec:
  type: ClusterIP
  selector:
    app: piggybank-service
  ports:
    - port: 8080
      targetPort: http
